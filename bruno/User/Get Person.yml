info:
  name: Get Person
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4556/peepol-api/persons/1/detail
  headers:
    - name: Authorization
      value: Bearer [token]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
