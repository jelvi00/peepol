info:
  name: Get Persons
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:4556/peepol-api/persons
  headers:
    - name: Authorization
      value: Bearer [token]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
