info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4556/peepol-api/auth/login
  body:
    type: json
    data: |-
      {
        "username": "aricardo",
        "password": "Peepol01!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
